# Why I'm Switching From Mailchimp to Flodesk in 2026

Creator: Eric Tech
Date: 2026-06-20
URL: https://www.youtube.com/watch?v=X1XwQm5-fJY

## Transcript

Every founder says they're building a simple wait list. And somehow it turns into five different tools stitched together with webhooks and automation. Mailchimp for the emails, a separate checkout tool, a standalone form builder, all duct taped together until something breaks. Flodesk is trying to solve that by combining lead capture, email automation, forms, checkout flows, and developer tooling into a single platform. Instead of spending hours wiring together different services, the idea is that you can build the entire customer acquisition pipeline in one place and still have access to APIs, webhooks, and the backend integrations you'd expect as a developer. So in this video, we're going to build a complete SaaS wait list and automated onboarding system from scratch. We'll embed a Flodesk form into a frontend, automatically trigger a welcome sequence when somebody signs up, and set up a webhook listener that instantly pushes subscriber data into our own application without pulling an API every few minutes. Let's start by building the frontend that people are actually going to see. The goal here isn't just to collect emails. We want a proper waitlist page that can capture leads, trigger automations, and eventually feed data back into our own application. So first, let's head over to Flodesk and create a new form. Right away, you'll notice this isn't one of those bare-bones email capture forms that looks completely disconnected from the rest of your product. Everything is visual, so you can customize the layout, branding, typography, colors, and the actual information you want to collect. And this is where Flodesk's biggest differentiator shows up. Unlike most email tools, Flodesk is built around your brand. Your fonts, colors, and visual identity carry through every email, form, and checkout page, so the whole funnel feels like one product instead of a patchwork. That polish isn't just cosmetic, either. Flodesk forms convert at roughly two times the industry benchmark, which matters a lot when you're paying for traffic to a wait list. Now, this might seem like a small detail, but it's actually one of the reasons these systems become a headache later on. Most founders build a landing page in one tool, a form in another, then spend time trying to make everything feel consistent. Here, the form, emails, landing pages, and automations all live inside the same ecosystem. So, you're not constantly jumping between platforms. For this example, I'm going to create a simple SaaS waitlist form. We'll collect the user's email address, their name, and I'll also add a custom field asking what they're interested in. This becomes useful later because those values can flow through automations, webhooks, and segmentation without us having to manually process anything. Once the form is ready, click on embed, and Flodesk will generate two snippets for us. The first is a global script that loads the rendering engine, and the second is the actual embed code for the form itself. Now, here's where the developer side comes in. I'm using a simple next.js application, but the process is basically the same whether you're using React, Webflow, or another front-end framework. We take the global script and place it inside the site's head section. Then, we drop the embed snippet wherever we want the form to appear. After refreshing the page, that's it. The form is now live inside our application. The next thing we need is onboarding because capturing an email without following up is basically leaving users sitting in limbo. So, let's head back into Flodesk and create an automation. This is where we define what happens the moment somebody joins our waitlist. We'll start by creating a workflow and selecting our waitlist form as the trigger. Now, anytime somebody submits that form, Flodesk automatically starts the workflow for that specific user. The first thing I want to do is send a welcome email. I'll add an email step here and choose one of the templates as a starting point. You can customize everything if you want, but for this demo, I'll keep it pretty simple. What's interesting here is that email is still surprisingly difficult under the hood. Different clients render content differently, custom fonts are often unsupported, and layouts can break depending on where the message is opened. Flodesk uses its own layout technology to render custom fonts consistently across Gmail, Outlook, Apple Mail, and mobile, something most email platforms can't reliably do. So, you're not manually fighting each client every time you send a campaign. And it pays off where it counts. Flodesk maintains about 99% deliverability, nearly 10 points above the industry average. So, the emails you design actually land in the inbox. There's also a counterintuitive point worth calling out. Flodesk has confirmed that beautifully designed, image-rich emails don't hurt deliverability, which goes against conventional wisdom in email marketing, where the usual advice is to keep emails plain and text-heavy. For the actual email, I'll include a welcome message, explain what the product does, and let users know they're officially on the waitlist. We can also personalize the content using subscriber data collected from the form. So, if we captured their name earlier, that can be inserted automatically. Now, let's make the workflow a little more useful. Instead of stopping after one email, I'll add a short onboarding sequence. Maybe one email goes out immediately, another goes out a day later with additional information about the product, and a third email shares updates or early access details. The nice thing is that once this workflow is active, every new sign-up automatically enters the sequence. So, the onboarding flow is working, but most founders don't just want subscribers sitting inside a marketing platform. Usually, you need that data inside your own application as well, whether that's a user database, analytics system, CRM, or some internal dashboard. A common way to solve this is polling an API every few minutes to check for new sign-ups, but that's honestly pretty inefficient. You're constantly making requests just to ask if anything changed. Instead, we're going to use a webhook. So, inside our back end, I'm going to create a simple API endpoint that listens for incoming webhook events. In my case, this is a Next.js route, but you could do the exact same thing with Express, Fastify, Nest.js, or pretty much any backend framework. Once the endpoint is ready, we'll head over to Flodesk and create a webhook subscription. And subscribe to the subscriber.created event. Now, whenever somebody joins our waitlist, Flodesk automatically sends a post request to our server. What's particularly nice here is that the webhook contains the actual subscriber data we need. A lot of systems send a tiny notification with just an ID, and then you're forced to make another API call to fetch the full record. That means more requests, more latency, and more complexity. Here, the payload already includes information like the subscriber's email, status, custom fields, and other metadata associated with that sign-up event. So, inside our handler, all we need to do is parse the payload and write the data into our database. In this example, I'm storing the user's email, the custom fields they submitted through the form, and their sign-up information inside our application. Now, let's test the full flow. I'll go back to the front end and submit the waitlist form. A few seconds later, we can see the subscriber appear inside Flodesk, the welcome sequence gets triggered automatically, and at the same time our webhook fires and updates our back-end database. No polling, no scheduled jobs, and no extra synchronization layer sitting in between. Now that we have the core flow working, the nice thing is that this doesn't have to stop at a simple waitlist. Most products eventually need additional steps after sign-up. Maybe you want to segment users based on their interests, send different onboarding sequences, give early access to beta users, or even start accepting payments before the product launches. Because everything is already connected, extending the system becomes pretty straightforward. For example, remember that custom field we added earlier when users joined the waitlist? We can use that information to automatically organize subscribers into different groups. If someone says they're interested in AI features, they can receive one onboarding path. If they're interested in automation or developer tooling, they can receive a completely different sequence. And if you're selling something, Flodesk also has a native checkout functionality built into the platform. So, instead of connecting a separate landing page builder, checkout provider, email platform, and automation tool, you can keep the entire customer journey inside the same system. And because it's brand first, the checkout page looks like the rest of your product instead of a generic payment screen. That's especially useful for founders launching courses, digital products memberships consulting offers, or even early access SaaS plans. Someone can discover the product, join the waitlist, receive onboarding emails, make a purchase, and enter a customer workflow without you having to stitch together multiple services. The other thing I like is that all of this still plays nicely with a developer workflow. If you need custom application logic, you can use the API. If you need real-time updates, you can use webhooks. And if all you need is a fast way to launch and validate an idea, you can rely on the built-in forms, automations, and checkout flows without building everything yourself. So, what started as a simple waitlist has now turned into a foundation you can keep building on as your product grows. And that's pretty much the complete system. Normally, a setup like this ends up spread across multiple services with a bunch of custom glue code holding everything together. But, here we were able to keep the entire workflow in one place while still having access to the APIs and integrations you'd expect as a developer. If you want to build something similar yourself, you can check out Flowdesk using the link in the description below. Whether you're launching a SaaS product, collecting early access sign-ups, selling digital products, or just trying to simplify your onboarding stack, it's a pretty practical way to get the whole pipeline running without stitching together a bunch of separate tools. That's it for this video. If you found it useful, don't forget to like and subscribe, and I'll see you in the next one.
